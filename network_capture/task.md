# Перехват трафика

Мы перехватили сетевой трафик подозрительного обмена данными между двумя системами.

В дампе содержится много различного сетевого трафика, но где-то среди всего этого шума скрыта передача секретных данных

**Формат флага:** `ctf{...}`
