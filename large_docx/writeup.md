## large_docx — разбор

1. DOCX — это ZIP-архив, только с другим расширением.
2. Переименовываем файл `challenge.docx` в `challenge.zip` или открываем его любым архиватором.
3. Внутри архива лежит файл `flag.txt`.
4. Открываем `flag.txt` и читаем флаг.

